<?xml version="1.0" encoding="utf-8"?>
<!-- Shared manifest: permissions, service, and boot receiver that both the
     mobile and tv flavors need.  Each flavor overlays its own <activity>
     + theme + (for tv) banner via src/<flavor>/AndroidManifest.xml. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Doze / battery optimisation exemption.  Without this Android kills
         long-lived TCP sockets (e.g. grammers MTProto to Telegram, or HLS
         upstream fetches) after ~15 s of backgrounded idle, even while the
         foreground-service notification is still visible.  The app prompts
         the user to grant this on first-start of the proxy. -->
    <uses-permission
        android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Keeps the CPU awake for ongoing network I/O during screen-off — the
         foreground service takes this programmatically only when it's
         actively streaming. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Required to launch the system package installer for in-app APK updates. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- FileProvider for ad-hoc exports (logs, config JSON) via share sheet -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- Background proxy service -->
        <service
            android:name=".ProxyService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Auto-start on boot -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
